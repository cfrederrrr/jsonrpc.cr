# stdlib dependencies
require "json"

# local dependencies
require "./scenario"

module JSON
  module RPC

    # A String specifying the version of the JSON-RPC protocol.
    # MUST be exactly "2.0"
    #
    VERSION2 = "2.0"

  end
end
